/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add your config options here
  reactStrictMode: true,
  // experimental: { appDir: true }, // or other flags if needed
};

module.exports = nextConfig;
